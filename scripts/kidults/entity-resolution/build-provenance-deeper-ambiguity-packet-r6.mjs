import fs from 'node:fs/promises';
import { createHash } from 'node:crypto';

const [capacityPath,samplingPath,contractPath,outPath='/tmp/provenance-deeper-ambiguity-packet-r6.json']=process.argv.slice(2);
if(!capacityPath||!samplingPath||!contractPath) throw new Error('usage: build-provenance-deeper-ambiguity-packet-r6 <capacity> <sampling> <contract> [out]');
const capacity=JSON.parse(await fs.readFile(capacityPath,'utf8'));
const sampling=JSON.parse(await fs.readFile(samplingPath,'utf8'));
const contract=JSON.parse(await fs.readFile(contractPath,'utf8'));
const hex=/^sha256:[a-f0-9]{64}$/;const sha=v=>`sha256:${createHash('sha256').update(typeof v==='string'?v:JSON.stringify(v)).digest('hex')}`;
if(capacity.id!=='kidults-er-provenance-deeper-ambiguity-r6'||capacity.stratum_id!=='er-stratum-provenance-unique-object'||capacity.rights_state!=='ALLOW') throw new Error('CAPACITY_INVALID');
if(contract.status!=='READY_FOR_REAL_REVIEWERS_NOT_ATTESTED') throw new Error('REVIEW_CONTRACT_NOT_READY');
const target=(sampling.strata||[]).find(x=>x.stratum_id===capacity.stratum_id);if(!target||target.case_class_targets?.AMBIGUOUS_REVIEW_REQUIRED!==50||target.cases!==120) throw new Error('SAMPLING_TARGET_INVALID');
const pool=(capacity.candidate_pool||[]).slice(0,4);
const cases=pool.map((c,i)=>{if(c.case_class!=='AMBIGUOUS_REVIEW_REQUIRED'||c.rights_state!=='ALLOW'||!hex.test(c.activity_payload_sha256)||!Array.isArray(c.object_references)||c.object_references.length<2||!Array.isArray(c.object_payload_sha256)||c.object_payload_sha256.some(x=>!hex.test(x.payload_sha256)))throw new Error('EVIDENCE_INVALID');return {case_id:`provenance-r6-ambiguous-${String(i+1).padStart(3,'0')}`,stratum_id:capacity.stratum_id,case_class:'AMBIGUOUS_REVIEW_REQUIRED',identity_boundary:'MARKET_EVENT',source_a_reference:c.activity_reference,source_a_payload_sha256:c.activity_payload_sha256,source_b_references:c.object_references,source_b_payload_sha256:c.object_payload_sha256,license_evidence_refs:c.license_evidence_refs,rights_state:'ALLOW',provenance_refs:c.provenance_refs,reviewer_prompt_context:{candidate_basis:c.candidate_basis,explicit_target_count:c.explicit_target_count},label:null,model_prediction:null,reviewer_assignment:'PENDING_REAL_REVIEWER'};});
const used=new Set();for(const c of cases)for(const r of [c.source_a_reference,...c.source_b_references]){if(used.has(r))throw new Error(`SOURCE_REFERENCE_REUSE:${r}`);used.add(r);}
const blindCount=Math.floor(cases.length/2),blindIds=cases.filter((_,i)=>i%2===0).slice(0,blindCount).map(x=>x.case_id);
const artifact={id:'kidults-er-provenance-deeper-ambiguity-packet-r6',version:'6.0.0',status:cases.length===4?'FULL_STRATUM_REVIEW_MATERIAL_READY_UNLABELED_NOT_REVIEWED':(cases.length?'PARTIAL_REVIEW_PACKET_READY_UNLABELED_NOT_REVIEWED':'NO_ADDITIONAL_CASES_PROVEN'),stratum_id:capacity.stratum_id,packet_scope:'PAGES_481_720_FINAL_AMBIGUITY_ONLY',case_count:cases.length,prior_reviewer_ready_case_count:116,total_reviewer_ready_if_combined:116+cases.length,sampling_target_total:120,remaining_case_count:4-cases.length,case_class_counts:{AMBIGUOUS_REVIEW_REQUIRED:cases.length},identity_boundary_counts:{MARKET_EVENT:cases.length},labels_present:false,model_predictions_present:false,reviewer_a:'NOT_ASSIGNED',reviewer_b:'NOT_ASSIGNED',blind_partition:{state:'CANDIDATE_NOT_SEALED',case_count:blindIds.length,case_ids:blindIds,partition_sha256:sha(blindIds)},cases,empirical_pass:false,track_b:'NOT_STARTED',public_release:'HOLD',production:'HOLD',truth_boundary:`${cases.length} final Getty source-bound AMBIGUOUS_REVIEW_REQUIRED cases from pages 481-720 are packetized after excluding every reference consumed by the prior 116 Provenance reviewer-ready cases. Reviewer material may reach 120/120, but labels, adjudication, sealed holdout, attestation, benchmark PASS, Track B, publication and Production remain blocked.`};
await fs.writeFile(outPath,JSON.stringify(artifact,null,2)+'\n');console.log(JSON.stringify({status:artifact.status,case_count:cases.length,total_provenance_reviewer_ready:artifact.total_reviewer_ready_if_combined,remaining:artifact.remaining_case_count,blind_candidates:blindIds.length,production:'HOLD'},null,2));
