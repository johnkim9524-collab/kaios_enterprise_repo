import fs from 'node:fs/promises';
const [planPath, samplingPath, serializedCapacityPath] = process.argv.slice(2);
if (!planPath || !samplingPath || !serializedCapacityPath) throw new Error('usage: validate-empirical-acquisition-wave1-r1.mjs <wave1.json> <sampling.json> <serialized-capacity.json>');
const plan = JSON.parse(await fs.readFile(planPath, 'utf8'));
const sampling = JSON.parse(await fs.readFile(samplingPath, 'utf8'));
const serializedCapacity = JSON.parse(await fs.readFile(serializedCapacityPath, 'utf8'));
if (plan.id !== 'kidults-er-empirical-acquisition-wave1-r1') throw new Error('WAVE1_ID_INVALID');
if (plan.status !== 'ACTIVE_PARTIAL_ACQUISITION') throw new Error('WAVE1_STATUS_INVALID');
if (plan.production !== 'HOLD' || plan.public_release !== 'HOLD') throw new Error('WAVE1_RELEASE_BOUNDARY_REQUIRED');
if (sampling.dataset_target?.total_cases !== 840 || sampling.dataset_target?.blind_holdout_cases !== 420) throw new Error('SAMPLING_840_420_REQUIRED');
if (plan.operating_target?.total_cases !== 840 || plan.operating_target?.blind_holdout !== 420 || plan.operating_target?.per_stratum !== 120 || plan.operating_target?.per_stratum_blind !== 60) throw new Error('WAVE1_TARGET_MISMATCH');
if (!Array.isArray(plan.lanes) || plan.lanes.length !== 7) throw new Error('SEVEN_LANES_REQUIRED');
const sampleIds = new Set((sampling.strata || []).map(x => x.stratum_id));
for (const lane of plan.lanes) { if (!sampleIds.has(lane.stratum_id)) throw new Error(`UNKNOWN_STRATUM:${lane.stratum_id}`); if (lane.target_cases !== 120) throw new Error(`TARGET_120_REQUIRED:${lane.stratum_id}`); }
const ready = plan.lanes.filter(x => x.state === 'START_READY');
const blocked = plan.lanes.filter(x => x.state !== 'START_READY');
const readyCapacity=ready.reduce((n,x)=>n+x.target_cases,0);
const blockedCapacity=blocked.reduce((n,x)=>n+x.target_cases,0);
if (plan.parallel_execution?.start_ready_case_capacity !== readyCapacity || plan.parallel_execution?.blocked_or_conditional_case_capacity !== blockedCapacity || readyCapacity+blockedCapacity!==840) throw new Error('DERIVED_CAPACITY_TRUTH_MISMATCH');
const pressing = plan.lanes.find(x=>x.stratum_id==='er-stratum-pressing-edition-media');
if (!pressing || pressing.state!=='START_READY' || !pressing.source_families.includes('musicbrainz-core-database-dump-cc0') || !pressing.rule.includes('exclude supplementary/derived data')) throw new Error('MUSICBRAINZ_CC0_CORE_BOUNDARY_REQUIRED');
const serialized=plan.lanes.find(x=>x.stratum_id==='er-stratum-serialized-reference');
if (serializedCapacity.id!=='kidults-er-serialized-reference-live-capacity-r1' || serializedCapacity.stratum_id!==serialized?.stratum_id) throw new Error('SERIALIZED_CAPACITY_ARTIFACT_BINDING_REQUIRED');
if (serialized.capacity_evidence!=='coordination/kidults/entity-resolution/serialized-reference-capacity-probe-r1.json') throw new Error('SERIALIZED_CAPACITY_EVIDENCE_PATH_REQUIRED');
const serializedReady=serializedCapacity.readiness_gate?.source_capacity_ready_for_120_cases===true;
if (serialized.state!==(serializedReady?'START_READY':'SOURCE_FIT_REVALIDATION_REQUIRED')) throw new Error('SERIALIZED_LANE_STATE_MUST_FOLLOW_CAPACITY_EVIDENCE');
if (!serialized.source_families.includes('wikidata-cc0-p2598-p217-strict-capacity-probe')) throw new Error('SERIALIZED_STRICT_SOURCE_FAMILY_REQUIRED');
if (!serialized.rule.includes('P2598') || !serialized.rule.includes('P217') || !serialized.rule.includes('P195') || !serialized.rule.includes('Met accessionNumber alone contributes zero capacity')) throw new Error('SERIALIZED_STRICT_GRAMMAR_AND_MET_EXCLUSION_REQUIRED');
const sm=serializedCapacity.metrics;
if (serialized.observed_capacity?.grammar_complete_real_records!==sm?.grammar_complete_real_record_count || serialized.observed_capacity?.cross_authority_alias_pairs!==sm?.cross_authority_alias_pair_count || serialized.observed_capacity?.same_model_distinct_serial_hard_negative_pairs!==sm?.same_model_distinct_serial_hard_negative_pair_count || serialized.observed_capacity?.conservative_case_capacity!==sm?.conservative_case_capacity || serialized.observed_capacity?.source_capacity_ready_for_120_cases!==serializedReady) throw new Error('SERIALIZED_OBSERVED_CAPACITY_SYNC_REQUIRED');
if (!serializedReady && (!serialized.blocker?.includes('Met accessionNumber alone is not a manufacturer serial or product reference') || readyCapacity!==360 || blockedCapacity!==480)) throw new Error('SERIALIZED_INSUFFICIENT_CAPACITY_MUST_FAIL_CLOSED');
if (!plan.lanes.some(x => x.stratum_id === 'er-stratum-graded-population' && x.state === 'BLOCKED_BULK_RIGHTS')) throw new Error('GRADED_BULK_RIGHTS_FAIL_CLOSED_REQUIRED');
const vehicle=plan.lanes.find(x=>x.stratum_id==='er-stratum-vehicle-mechanical-asset');
if (!vehicle?.blocker?.includes('NHTSA vPIC bulk VIN API use prohibited')) throw new Error('NHTSA_BULK_VIN_BLOCK_REQUIRED');
if (plan.parallel_execution?.reviewer_a !== 'NOT_ASSIGNED' || plan.parallel_execution?.reviewer_b !== 'NOT_ASSIGNED') throw new Error('REVIEWER_IDENTITY_MUST_NOT_BE_FABRICATED');
if (plan.parallel_execution?.labels_collected !== 0 || plan.parallel_execution?.blind_partition_sealed !== false || plan.parallel_execution?.empirical_attestation_created !== false || plan.parallel_execution?.track_b_started !== false) throw new Error('DOWNSTREAM_MUST_REMAIN_BLOCKED');
console.log(`PASS: Wave 1 has ${readyCapacity} start-ready case targets and fails closed on ${blockedCapacity}; SERIALIZED_REFERENCE follows strict live capacity evidence and Met accessionNumber alone contributes zero capacity.`);
