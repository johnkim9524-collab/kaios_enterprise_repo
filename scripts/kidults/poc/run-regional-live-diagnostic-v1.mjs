#!/usr/bin/env node
import fs from 'node:fs';import path from 'node:path';
const input=process.argv[2]||'coordination/kidults/scope-data/scope-poc-anchor-selection-v1.json';
const outDir=process.argv[3]||'tmp/regional-live-diagnostic-v1';fs.mkdirSync(outDir,{recursive:true});
const x=JSON.parse(fs.readFileSync(input,'utf8')),records=x.records||[];const sleep=ms=>new Promise(r=>setTimeout(r,ms));
async function j(url,attempt=0){const c=new AbortController(),t=setTimeout(()=>c.abort(),10000);try{const r=await fetch(url,{signal:c.signal,headers:{'User-Agent':'KIDULTS-Regional-Diagnostic/1.0'}});if((r.status===429||r.status>=500)&&attempt<4){const ra=Number(r.headers.get('retry-after')||0);await sleep(Math.max(ra*1000,800*(2**attempt)));return j(url,attempt+1)}if(!r.ok)throw new Error('HTTP_'+r.status);return await r.json()}finally{clearTimeout(t)}}
const targetSites={enwiki:'GLOBAL_EN',jawiki:'JAPAN_JA',kowiki:'KOREA_KO',dewiki:'GERMANY_DE',frwiki:'FRANCE_FR'};const rows=[],errors=[];
for(const r of records){let qid=null;try{const u=new URL('https://www.wikidata.org/w/api.php');for(const[k,v]of Object.entries({action:'wbsearchentities',search:r.display_name||r.representative_product_id,language:'en',uselang:'en',limit:'1',format:'json',origin:'*'}))u.searchParams.set(k,v);const s=await j(u);qid=s?.search?.[0]?.id||null}catch(e){errors.push({id:r.representative_product_id,stage:'SEARCH',error:e.message})}
let sitelinks={};if(qid){try{const u=new URL('https://www.wikidata.org/w/api.php');for(const[k,v]of Object.entries({action:'wbgetentities',ids:qid,props:'sitelinks',format:'json',origin:'*'}))u.searchParams.set(k,v);const d=await j(u);sitelinks=d?.entities?.[qid]?.sitelinks||{}}catch(e){errors.push({id:r.representative_product_id,stage:'SITELINKS',error:e.message})}}
const regional_presence=Object.entries(targetSites).filter(([site])=>sitelinks[site]).map(([site,region])=>({site,region,title:sitelinks[site].title}));rows.push({representative_product_id:r.representative_product_id,scope_id:r.target_scope_id,display_name:r.display_name,wikidata_qid:qid,regional_public_representation:regional_presence,source_family:'WIKIMEDIA_WIKIDATA_SINGLE_LINEAGE',regional_semantics:'PUBLIC_LANGUAGE_REGION_REPRESENTATION_ONLY_NOT_DEMAND_NOT_TRANSACTION',state:regional_presence.length>=2?'PUBLIC_REGIONAL_REPRESENTATION_OBSERVED_NOT_REGIONAL_MARKET_PROVEN':'LIMITED_PUBLIC_REGIONAL_REPRESENTATION'});await sleep(220)}
const mapped=rows.filter(r=>r.wikidata_qid).length,multi=rows.filter(r=>r.regional_public_representation.length>=2).length,scopes=new Set(rows.filter(r=>r.regional_public_representation.length>=2).map(r=>r.scope_id)).size;
const defects=[{id:'REGIONAL-INDEPENDENCE-GAP',severity:'P0',finding:'One Wikimedia/Wikidata lineage cannot satisfy the Regional contract requirement for two independent source families plus institutional/release/venue evidence.',remediation:'Add independent regional institutional/release/venue evidence family before any REGIONAL challenger selection.'},{id:'REGIONAL-SEMANTICS-GUARD',severity:'P0_GUARD',finding:'Language/region representation is not regional demand, liquidity or transaction evidence.'}];
const out={id:'kidults-regional-live-diagnostic-v1',status:'DIAGNOSTIC_COMPLETE_FAIL_CLOSED',products:records.length,mapped_products:mapped,products_with_multi_region_public_representation:multi,scopes_with_multi_region_public_representation:scopes,request_errors:errors,rows,defects,selected_regional_challengers:0,provider_contact:'HOLD',production:'HOLD'};fs.writeFileSync(path.join(outDir,'regional-live-diagnostic-v1.json'),JSON.stringify(out,null,2));console.log(JSON.stringify({status:out.status,products:out.products,mapped,multi_region:multi,scopes_multi_region:scopes,errors:errors.length,selected:0},null,2));
