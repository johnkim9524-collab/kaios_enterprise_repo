#!/usr/bin/env node
import fs from 'node:fs';const p=process.argv[2]||'coordination/kidults/provider/provider-contact-readiness-gate-v1.json';const x=JSON.parse(fs.readFileSync(p,'utf8'));const req=(v,m)=>{if(!v)throw new Error(m)};req(x.status==='PROGRAM_OWNER_DECISION_READY','status');req(x.contact_authorized===false,'contact hold');req(x.groups.HIGH_FIT_APPLICATION_OR_LICENSE_INQUIRY.length===3,'high fit');req(x.groups.RIGHTS_OR_BULK_ACCESS_INQUIRY.length>=5,'rights inquiry');req(x.groups.REJECT_FOR_SOLD_TRANSACTION_EVIDENCE.some(x=>x.provider.includes('StockX')),'stockx reject');req(x.groups.REJECT_FOR_SOLD_TRANSACTION_EVIDENCE.some(x=>x.provider.includes('TCGplayer')),'tcg reject');req(x.groups.DISCOVERY_GAP_REMAINS.length===8,'domain gaps');req(x.program_owner_decision.required===true&&x.program_owner_decision.default==='HOLD_ALL_CONTACTS','owner gate');req(x.guards.no_contact_without_owner_decision&&x.guards.no_contract_commitment_without_separate_approval&&x.guards.no_credentials_in_registry&&x.guards.no_production,'guards');req(x.production==='HOLD','production');console.log(JSON.stringify({status:'PASS',high_fit:3,rights_inquiry:x.groups.RIGHTS_OR_BULK_ACCESS_INQUIRY.length,rejected:x.groups.REJECT_FOR_SOLD_TRANSACTION_EVIDENCE.length,discovery_gaps:8,contact:'HOLD',production:'HOLD'},null,2));
