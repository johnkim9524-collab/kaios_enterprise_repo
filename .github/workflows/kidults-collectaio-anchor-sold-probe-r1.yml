name: KIDULTS CollectAIO Anchor SOLD Probe R1

on:
  workflow_dispatch:
  pull_request:
    paths:
      - 'scripts/kidults/source/run-collectaio-anchor-sold-probe-r1.mjs'
      - '.github/workflows/kidults-collectaio-anchor-sold-probe-r1.yml'

permissions:
  contents: read

jobs:
  bounded-public-probe:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - name: Run bounded public CollectAIO probe
        run: node scripts/kidults/source/run-collectaio-anchor-sold-probe-r1.mjs
      - name: Upload bounded evidence artifact
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: collectaio-anchor-sold-probe-r1
          path: artifacts/kidults/source/collectaio-anchor-sold-probe-r1.json
          if-no-files-found: error
