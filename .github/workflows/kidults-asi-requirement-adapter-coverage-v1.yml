name: KIDULTS ASI Requirement-to-Adapter Coverage v1
run-name: KIDULTS Coverage / ${{ github.event_name == 'workflow_run' && format('source-{0}', github.event.workflow_run.head_sha) || format('manual-{0}', github.run_id) }}

on:
  workflow_dispatch:
  workflow_run:
    workflows:
      - 'KIDULTS ASI Autonomous Resolution Layer v1'
    branches: [main]
    types: [completed]

permissions:
  contents: read
  actions: read

jobs:
  classify-upstream-arl-generation:
    runs-on: ubuntu-24.04
    timeout-minutes: 10
    permissions:
      contents: read
      actions: read
    outputs:
      should_run: ${{ steps.classify.outputs.should_run }}
      admission: ${{ steps.classify.outputs.admission }}
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
        with:
          persist-credentials: false
      - name: Classify exact upstream ARL generation
        id: classify
        env:
          GH_TOKEN: ${{ github.token }}
          EVENT_ARL_RUN_ID: ${{ github.event.workflow_run.id }}
          EVENT_ARL_RUN_ATTEMPT: ${{ github.event.workflow_run.run_attempt }}
        run: |
          set -euo pipefail
          if [ "$GITHUB_EVENT_NAME" = workflow_dispatch ]; then
            echo 'should_run=true' >> "$GITHUB_OUTPUT"
            echo 'admission=MANUAL_EXPLICIT' >> "$GITHUB_OUTPUT"
            exit 0
          fi
          test -n "$EVENT_ARL_RUN_ID"
          gh api -H 'Accept: application/vnd.github+json' "/repos/${GITHUB_REPOSITORY}/actions/runs/${EVENT_ARL_RUN_ID}" > /tmp/arl-admission-run.json
          ARL_EVENT=$(jq -r .event /tmp/arl-admission-run.json)
          if [ "$ARL_EVENT" != workflow_run ]; then
            node scripts/kidults/source-intelligence/classify-requirement-coverage-admission-v1.mjs /tmp/arl-admission-run.json - "$GITHUB_REPOSITORY" "$GITHUB_SHA" /tmp/coverage-admission-v1.json
            echo "should_run=$(jq -r .should_run /tmp/coverage-admission-v1.json)" >> "$GITHUB_OUTPUT"
            echo "admission=$(jq -r .admission /tmp/coverage-admission-v1.json)" >> "$GITHUB_OUTPUT"
            exit 0
          fi
          gh api -H 'Accept: application/vnd.github+json' "/repos/${GITHUB_REPOSITORY}/actions/runs/${EVENT_ARL_RUN_ID}/artifacts?per_page=100" > /tmp/arl-admission-artifacts.json
          EVENT_ARL_RUN_ID="$EVENT_ARL_RUN_ID" EVENT_ARL_RUN_ATTEMPT="$EVENT_ARL_RUN_ATTEMPT" node - <<'NODE'
          const fs=require('fs'); const x=JSON.parse(fs.readFileSync('/tmp/arl-admission-artifacts.json','utf8'));
          const name=`kidults-asi-arl-p1-generation-classification-v1-${process.env.EVENT_ARL_RUN_ID}-${process.env.EVENT_ARL_RUN_ATTEMPT}`;
          const matches=(x.artifacts||[]).filter(a=>a.name===name&&a.expired===false&&a.workflow_run?.id===Number(process.env.EVENT_ARL_RUN_ID));
          if(matches.length!==1)throw new Error(`ARL_CLASSIFICATION_ARTIFACT_CARDINALITY:${matches.length}`);
          fs.writeFileSync('/tmp/arl-admission-artifact.json', JSON.stringify(matches[0], null, 2));
          NODE
          ARTIFACT_ID=$(jq -r .id /tmp/arl-admission-artifact.json)
          ARTIFACT_DIGEST=$(jq -r .digest /tmp/arl-admission-artifact.json)
          [[ "$ARTIFACT_DIGEST" =~ ^sha256:[a-f0-9]{64}$ ]]
          gh api -H 'Accept: application/vnd.github+json' "/repos/${GITHUB_REPOSITORY}/actions/artifacts/${ARTIFACT_ID}/zip" > /tmp/arl-admission.zip
          python3 scripts/kidults/kpmo/validate-safe-zip-archive-v1.py --archive /tmp/arl-admission.zip --expected-digest "$ARTIFACT_DIGEST" --receipt /tmp/arl-admission-archive-receipt.json --max-compressed-bytes 1048576 --max-entries 4 --max-entry-uncompressed-bytes 1048576 --max-total-uncompressed-bytes 1048576 --max-compression-ratio 100 --required-basename arl-p1-generation-classification-v1.json
          mkdir -p /tmp/arl-admission-expanded
          unzip -q /tmp/arl-admission.zip -d /tmp/arl-admission-expanded
          mapfile -t RECEIPTS < <(find /tmp/arl-admission-expanded -type f -name 'arl-p1-generation-classification-v1.json' -print)
          test "${#RECEIPTS[@]}" -eq 1
          node scripts/kidults/source-intelligence/classify-requirement-coverage-admission-v1.mjs /tmp/arl-admission-run.json "${RECEIPTS[0]}" "$GITHUB_REPOSITORY" "$GITHUB_SHA" /tmp/coverage-admission-v1.json
          echo "should_run=$(jq -r .should_run /tmp/coverage-admission-v1.json)" >> "$GITHUB_OUTPUT"
          echo "admission=$(jq -r .admission /tmp/coverage-admission-v1.json)" >> "$GITHUB_OUTPUT"
      - name: Retain exact Coverage admission receipt
        if: always()
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02
        with:
          name: kidults-asi-requirement-coverage-admission-v1-${{ github.run_id }}-${{ github.run_attempt }}
          path: /tmp/coverage-admission-v1.json
          retention-days: 90
          if-no-files-found: ${{ github.event_name == 'workflow_dispatch' && 'ignore' || 'error' }}

  verify-requirement-adapter-coverage:
    needs: classify-upstream-arl-generation
    if: needs.classify-upstream-arl-generation.outputs.should_run == 'true'
    concurrency:
      group: kidults-asi-requirement-adapter-coverage-v1-${{ github.event_name == 'workflow_run' && format('{0}-{1}', github.event.workflow_run.head_sha, 'ASI_AUTONOMOUS_RESOLUTION') || github.run_id }}
      cancel-in-progress: false
    runs-on: ubuntu-24.04
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
        with:
          fetch-depth: 0
          persist-credentials: false
      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020
        with:
          node-version: '24.19.0'
      - name: Install locked dependencies
        run: npm ci --ignore-scripts --no-audit --no-fund
      - name: Verify exact source and coverage syntax
        run: |
          set -euo pipefail
          test "$(git rev-parse HEAD)" = "$GITHUB_SHA"
          node scripts/kidults/kpmo/validate-requirement-coverage-dependency-bootstrap-v1.mjs
          node --test tests/kidults/kpmo/requirement-coverage-dependency-bootstrap-v1.test.mjs
          node --check scripts/kidults/source-intelligence/build-asi-requirement-adapter-coverage-v1.mjs
          node --check scripts/kidults/source-intelligence/validate-asi-requirement-adapter-coverage-v1.mjs
          node --check scripts/kidults/source-intelligence/lib/source-purpose-rights-gate-v1.mjs
          node --check scripts/kidults/source-intelligence/validate-asi-requirement-adapter-coverage-registry-v1.mjs
          node --check scripts/kidults/source-intelligence/resolve-asi-requirement-adapter-coverage-canonical-guard-v1.mjs
          node --check scripts/kidults/source-intelligence/build-asi-requirement-adapter-coverage-semantic-input-v1.mjs
          node --check scripts/kidults/source-intelligence/resolve-asi-orchestration-run-history-v1.mjs
          node --check scripts/kidults/source-intelligence/classify-requirement-coverage-admission-v1.mjs
          node --check scripts/kidults/source-intelligence/validate-asi-orchestration-run-history-v1.mjs
          python3 -m py_compile scripts/kidults/kpmo/validate-safe-zip-archive-v1.py scripts/kidults/kpmo/validate-safe-zip-archive-v1.test.py
          python3 scripts/kidults/kpmo/validate-safe-zip-archive-v1.test.py
          node scripts/kidults/source-intelligence/validate-asi-requirement-adapter-coverage-canonical-guard-v1.mjs
          node scripts/kidults/source-intelligence/validate-asi-orchestration-run-history-v1.mjs
          node --test tests/kidults/source-intelligence/requirement-coverage-admission-v1.test.mjs
          node scripts/kidults/redteam/validate-artifact-consumer-orchestration-v1.mjs
          node -e "JSON.parse(require('fs').readFileSync('coordination/kidults/source-intelligence/asi-requirement-adapter-coverage-contract-v1.json','utf8'))"
          node -e "JSON.parse(require('fs').readFileSync('coordination/kidults/source-intelligence/asi-requirement-adapter-coverage-registry-v1.json','utf8'))"
          node -e "JSON.parse(require('fs').readFileSync('coordination/kidults/schemas/asi-autonomous-resolution-artifact-binding-v1.schema.json','utf8'))"
      - name: Validate requirement coverage registry and automatic continuation
        run: |
          set -euo pipefail
          node scripts/kidults/source-intelligence/validate-asi-requirement-adapter-coverage-registry-v1.mjs \
            | tee /tmp/kidults-asi-requirement-adapter-coverage-registry-validation-v1.json
      - name: Restore exact artifact-producing autonomous-resolution run
        env:
          GH_TOKEN: ${{ github.token }}
          TARGET_SOURCE_SHA: ${{ github.event.workflow_run.head_sha || github.event.pull_request.head.sha || github.sha }}
          EXPECTED_HEAD_BRANCH: ${{ github.event.pull_request.head.ref || 'main' }}
          ARTIFACT_VALIDATION_SCOPE: ${{ github.event_name == 'pull_request' && 'PULL_REQUEST_HEAD' || 'MAIN' }}
          EVENT_ARL_RUN_ID: ${{ github.event.workflow_run.id }}
          EVENT_ARL_EVENT: ${{ github.event.workflow_run.event }}
          EVENT_ARL_RUN_ATTEMPT: ${{ github.event.workflow_run.run_attempt }}
          EVENT_ARL_HEAD_REPOSITORY: ${{ github.event.workflow_run.head_repository.full_name }}
        run: |
          set -euo pipefail
          mkdir -p /tmp/arl-expanded /tmp/arl-input
          rm -f /tmp/arl-run.json /tmp/arl-artifact.json

          if [ "$GITHUB_EVENT_NAME" = workflow_run ]; then
            test -n "$EVENT_ARL_RUN_ID"
            test "$EVENT_ARL_EVENT" != push
            test "$EVENT_ARL_HEAD_REPOSITORY" = "$GITHUB_REPOSITORY"
            RUN_ID="$EVENT_ARL_RUN_ID"
            gh api -H 'Accept: application/vnd.github+json' \
              "/repos/${GITHUB_REPOSITORY}/actions/runs/${EVENT_ARL_RUN_ID}" \
              > /tmp/arl-run.json
            EVENT_ARL_RUN_ID="$EVENT_ARL_RUN_ID" EVENT_ARL_EVENT="$EVENT_ARL_EVENT" TARGET_SOURCE_SHA="$TARGET_SOURCE_SHA" EXPECTED_HEAD_BRANCH="$EXPECTED_HEAD_BRANCH" node --input-type=module - <<'NODE'
          import fs from 'node:fs';
          import { nativeWorkflowRunNameMatches } from './scripts/kidults/source-intelligence/native-workflow-run-identity-v1.mjs';
          const run=JSON.parse(fs.readFileSync('/tmp/arl-run.json','utf8'));
          if(run.id!==Number(process.env.EVENT_ARL_RUN_ID))throw new Error('AUTONOMOUS_RESOLUTION_TRIGGER_RUN_ID_MISMATCH');
          if(!nativeWorkflowRunNameMatches(run,'KIDULTS ASI Autonomous Resolution Layer v1','.github/workflows/kidults-asi-autonomous-resolution-layer-v1.yml'))throw new Error('AUTONOMOUS_RESOLUTION_TRIGGER_IDENTITY');
          if(run.repository?.full_name!==process.env.GITHUB_REPOSITORY||run.head_repository?.full_name!==process.env.GITHUB_REPOSITORY)throw new Error('AUTONOMOUS_RESOLUTION_NATIVE_REPOSITORY');
          if(run.run_attempt!==Number(process.env.EVENT_ARL_RUN_ATTEMPT))throw new Error('AUTONOMOUS_RESOLUTION_TRIGGER_ATTEMPT_MISMATCH');
          if(run.head_sha!==process.env.TARGET_SOURCE_SHA||run.head_branch!==process.env.EXPECTED_HEAD_BRANCH)throw new Error('AUTONOMOUS_RESOLUTION_TRIGGER_SOURCE_SCOPE');
          if(run.status!=='completed'||run.conclusion!=='success')throw new Error('AUTONOMOUS_RESOLUTION_TRIGGER_NOT_SUCCESS');
          if(run.event!==process.env.EVENT_ARL_EVENT||run.event!=='workflow_run')throw new Error(`AUTONOMOUS_RESOLUTION_NON_ARTIFACT_EVENT:${run.event}`);
          NODE
          else
            for ATTEMPT in $(seq 1 24); do
              gh api -H 'Accept: application/vnd.github+json' \
                "/repos/${GITHUB_REPOSITORY}/actions/workflows/kidults-asi-autonomous-resolution-layer-v1.yml/runs?head_sha=${TARGET_SOURCE_SHA}&status=success&per_page=100" \
                > /tmp/arl-runs.json
              TARGET_SOURCE_SHA="$TARGET_SOURCE_SHA" EXPECTED_HEAD_BRANCH="$EXPECTED_HEAD_BRANCH" node --input-type=module - <<'NODE'
          import fs from 'node:fs';
          import { nativeWorkflowRunNameMatches } from './scripts/kidults/source-intelligence/native-workflow-run-identity-v1.mjs';
          const x=JSON.parse(fs.readFileSync('/tmp/arl-runs.json','utf8'));
          const target=process.env.TARGET_SOURCE_SHA;
          const expectedBranch=process.env.EXPECTED_HEAD_BRANCH;
          const artifactProducingEvents=new Set(['workflow_run']);
          const runs=(x.workflow_runs||[])
            .filter((run)=>nativeWorkflowRunNameMatches(run,'KIDULTS ASI Autonomous Resolution Layer v1','.github/workflows/kidults-asi-autonomous-resolution-layer-v1.yml')&&
              run.repository?.full_name===process.env.GITHUB_REPOSITORY&&run.head_repository?.full_name===process.env.GITHUB_REPOSITORY&&
              run.head_sha===target&&run.head_branch===expectedBranch&&
              run.status==='completed'&&run.conclusion==='success'&&
              run.event!=='push'&&artifactProducingEvents.has(run.event))
            .sort((a,b)=>Date.parse(b.created_at)-Date.parse(a.created_at)||b.id-a.id);
          if(runs.length)fs.writeFileSync('/tmp/arl-run.json',JSON.stringify(runs[0],null,2));
          NODE
              if [ -s /tmp/arl-run.json ]; then break; fi
              if [ "$ATTEMPT" -eq 24 ]; then
                echo "NO_SUCCESSFUL_ARTIFACT_PRODUCING_SAME_SHA_AUTONOMOUS_RESOLUTION_RUN:${TARGET_SOURCE_SHA}" >&2
                exit 1
              fi
              sleep 10
            done
            RUN_ID=$(node -p "JSON.parse(require('fs').readFileSync('/tmp/arl-run.json')).id")
          fi

          test -n "$RUN_ID"
          for ATTEMPT in $(seq 1 12); do
            rm -f /tmp/arl-artifact.json
            gh api -H 'Accept: application/vnd.github+json' \
              "/repos/${GITHUB_REPOSITORY}/actions/runs/${RUN_ID}/artifacts?per_page=100" \
              > /tmp/arl-artifacts.json
            node - <<'NODE'
          const fs=require('fs');
          const run=JSON.parse(fs.readFileSync('/tmp/arl-run.json','utf8'));
          const x=JSON.parse(fs.readFileSync('/tmp/arl-artifacts.json','utf8'));
          const artifacts=(x.artifacts||[])
            .filter((artifact)=>artifact.name==='kidults-asi-autonomous-resolution-layer-v1'&&
              artifact.expired===false&&artifact.workflow_run?.id===run.id&&
              artifact.workflow_run?.head_sha===run.head_sha);
          if(artifacts.length>1)throw new Error(`AUTONOMOUS_RESOLUTION_ARTIFACT_CARDINALITY:${artifacts.length}`);
          if(artifacts.length===1)fs.writeFileSync('/tmp/arl-artifact.json',JSON.stringify(artifacts[0],null,2));
          NODE
            if [ -s /tmp/arl-artifact.json ]; then break; fi
            if [ "$ATTEMPT" -eq 12 ]; then
              echo "AUTONOMOUS_RESOLUTION_ARTIFACT_NOT_AVAILABLE:${RUN_ID}" >&2
              exit 1
            fi
            sleep 5
          done

          ARTIFACT_ID=$(node -p "JSON.parse(require('fs').readFileSync('/tmp/arl-artifact.json')).id")
          ARTIFACT_DIGEST=$(node -p "JSON.parse(require('fs').readFileSync('/tmp/arl-artifact.json')).digest || ''")
          ARTIFACT_EXPIRES_AT=$(node -p "JSON.parse(require('fs').readFileSync('/tmp/arl-artifact.json')).expires_at || ''")
          [[ "$ARTIFACT_DIGEST" =~ ^sha256:[a-f0-9]{64}$ ]]
          ARTIFACT_EXPIRES_AT="$ARTIFACT_EXPIRES_AT" node -e "const expires=Date.parse(process.env.ARTIFACT_EXPIRES_AT);if(!Number.isFinite(expires)||expires<=Date.now())throw new Error('AUTONOMOUS_RESOLUTION_ARTIFACT_EXPIRY_INVALID')"
          gh api -H 'Accept: application/vnd.github+json' \
            "/repos/${GITHUB_REPOSITORY}/actions/artifacts/${ARTIFACT_ID}/zip" \
            > /tmp/arl.zip
          python3 scripts/kidults/kpmo/validate-safe-zip-archive-v1.py \
            --archive /tmp/arl.zip \
            --expected-digest "$ARTIFACT_DIGEST" \
            --receipt /tmp/arl-archive-validation-receipt-v1.json \
            --max-compressed-bytes 4194304 \
            --max-entries 256 \
            --max-entry-uncompressed-bytes 8388608 \
            --max-total-uncompressed-bytes 33554432 \
            --max-compression-ratio 100
          unzip -q -o /tmp/arl.zip -d /tmp/arl-expanded
          for NAME in \
            replacement-source-mission-queue-v1.json \
            autonomous-resolution-manifest-v1.json \
            kidults-asi-autonomous-resolution-layer-kpmo-receipt-v1.json; do
            mapfile -t FILES < <(find /tmp/arl-expanded -type f -name "$NAME" -print)
            test "${#FILES[@]}" -eq 1
            cp "${FILES[0]}" "/tmp/arl-input/$NAME"
          done
          node scripts/kidults/source-intelligence/resolve-asi-orchestration-run-history-v1.mjs \
            --mode coverage-exact-producer \
            --run /tmp/arl-run.json \
            --receipt /tmp/arl-input/kidults-asi-autonomous-resolution-layer-kpmo-receipt-v1.json \
            --source-sha "$TARGET_SOURCE_SHA" --head-branch "$EXPECTED_HEAD_BRANCH" \
            --output /tmp/coverage-exact-upstream-producer-receipt-v1.json
          AUTHORITATIVE_PRODUCER_CARDINALITY=$(jq -r '.authoritative_producer_cardinality' /tmp/coverage-exact-upstream-producer-receipt-v1.json)
          test "$AUTHORITATIVE_PRODUCER_CARDINALITY" = 1
          PRODUCER_HEAD_SHA=$(node -p "JSON.parse(require('fs').readFileSync('/tmp/arl-run.json')).head_sha")
          EXECUTION_SHA=$(node -p "JSON.parse(require('fs').readFileSync('/tmp/arl-input/kidults-asi-autonomous-resolution-layer-kpmo-receipt-v1.json')).source_sha")
          CONSUMER_SHA=$(git rev-parse HEAD)
          git merge-base --is-ancestor "$PRODUCER_HEAD_SHA" "$CONSUMER_SHA"
          git merge-base --is-ancestor "$EXECUTION_SHA" "$CONSUMER_SHA"
          export PRODUCER_HEAD_SHA EXECUTION_SHA CONSUMER_SHA AUTHORITATIVE_PRODUCER_CARDINALITY
          node - <<'NODE'
          const fs=require('fs');
          const run=JSON.parse(fs.readFileSync('/tmp/arl-run.json','utf8'));
          const artifact=JSON.parse(fs.readFileSync('/tmp/arl-artifact.json','utf8'));
          const expectedSourceSha=process.env.TARGET_SOURCE_SHA;
          const expectedHeadBranch=process.env.EXPECTED_HEAD_BRANCH;
          const validationScope=process.env.ARTIFACT_VALIDATION_SCOPE;
          const executionSha=process.env.EXECUTION_SHA;
          if(run.head_sha!==expectedSourceSha||run.head_sha!==process.env.PRODUCER_HEAD_SHA)throw new Error('AUTONOMOUS_RESOLUTION_SOURCE_SHA_SCOPE');
          if(run.head_branch!==expectedHeadBranch)throw new Error('AUTONOMOUS_RESOLUTION_HEAD_BRANCH_SCOPE');
          if(run.event!=='workflow_run')throw new Error(`AUTONOMOUS_RESOLUTION_NONAUTHORITATIVE_PRODUCER_EVENT:${run.event}`);
          if(!['MAIN','PULL_REQUEST_HEAD'].includes(validationScope))throw new Error('AUTONOMOUS_RESOLUTION_VALIDATION_SCOPE');
          const receipt=JSON.parse(fs.readFileSync('/tmp/arl-input/kidults-asi-autonomous-resolution-layer-kpmo-receipt-v1.json','utf8'));
          if(receipt.producer_workflow_run_id!==run.id||receipt.producer_display_title!==run.display_title)throw new Error('AUTONOMOUS_RESOLUTION_RECEIPT_PRODUCER_IDENTITY_MISMATCH');
          const upstreamClass='ASI_AUTONOMOUS_RESOLUTION';
          const canonicalRunKey=`${run.head_sha}:${upstreamClass}`;
          const binding={id:'kidults-asi-autonomous-resolution-artifact-binding-v1',version:'1.3.0',artifact_id:artifact.id,artifact_name:artifact.name,expired:artifact.expired,workflow_run_id:run.id,workflow_event:run.event,consumer_event:process.env.GITHUB_EVENT_NAME,workflow_name:'KIDULTS ASI Autonomous Resolution Layer v1',workflow_path:run.path,producer_display_title:run.display_title,authoritative_generation_key:receipt.authoritative_generation_key,authoritative_producer_cardinality:Number(process.env.AUTHORITATIVE_PRODUCER_CARDINALITY),upstream_class:upstreamClass,canonical_run_key:canonicalRunKey,head_branch:run.head_branch,expected_head_branch:expectedHeadBranch,head_sha:run.head_sha,expected_source_sha:expectedSourceSha,execution_sha:executionSha,consumer_sha:process.env.CONSUMER_SHA,validation_scope:validationScope,main_scope_validated:validationScope==='MAIN',production_authorized:false,exact_triggering_run_bound:process.env.GITHUB_EVENT_NAME==='workflow_run',authoritative_producer_event:run.event==='workflow_run',source_sha_ancestor_of_consumer:true,execution_sha_ancestor_of_consumer:true,status:run.status,conclusion:run.conclusion,created_at:artifact.created_at,expires_at:artifact.expires_at};
          fs.writeFileSync('/tmp/arl-input/autonomous-resolution-artifact-binding-v1.json',JSON.stringify(binding,null,2)+'\n');
          NODE
          echo "ARL_RUN_ID=$RUN_ID" >> "$GITHUB_ENV"
          echo "ARL_ARTIFACT_ID=$ARTIFACT_ID" >> "$GITHUB_ENV"
          echo "ARL_ARTIFACT_DIGEST=$ARTIFACT_DIGEST" >> "$GITHUB_ENV"
          echo "ARL_ARCHIVE_VALIDATION_RECEIPT_DIGEST=sha256:$(sha256sum /tmp/arl-archive-validation-receipt-v1.json | cut -d ' ' -f1)" >> "$GITHUB_ENV"
          echo "ARL_PRODUCER_HEAD_SHA=$PRODUCER_HEAD_SHA" >> "$GITHUB_ENV"
          echo "ARL_EXECUTION_SHA=$EXECUTION_SHA" >> "$GITHUB_ENV"
          echo "arl_run_id=$RUN_ID artifact_id=$ARTIFACT_ID producer_head_sha=$PRODUCER_HEAD_SHA execution_sha=$EXECUTION_SHA consumer_sha=$CONSUMER_SHA"
      - name: Resolve bounded Coverage canonical leader or alias
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          GUARD_DIR=/tmp/kidults-coverage-canonical-guard
          CANDIDATE_DIR="$GUARD_DIR/candidates"
          mkdir -p "$CANDIDATE_DIR"
          SOURCE_SHA="$ARL_PRODUCER_HEAD_SHA"
          UPSTREAM_CLASS=ASI_AUTONOMOUS_RESOLUTION
          CANONICAL_RUN_KEY="${SOURCE_SHA}:${UPSTREAM_CLASS}"
          CANONICAL_KEY_DIGEST=$(CANONICAL_RUN_KEY="$CANONICAL_RUN_KEY" node -e "const c=require('crypto');process.stdout.write(c.createHash('sha256').update(process.env.CANONICAL_RUN_KEY).digest('hex'))")
          CANONICAL_ARTIFACT_NAME="kidults-asi-requirement-adapter-coverage-canonical-${CANONICAL_KEY_DIGEST}"
          CANONICAL_CONTRACT_DIGEST="sha256:$(sha256sum coordination/kidults/source-intelligence/asi-requirement-adapter-coverage-contract-v1.json | cut -d ' ' -f1)"
          UPSTREAM_BINDING_DIGEST="sha256:$(sha256sum /tmp/arl-input/autonomous-resolution-artifact-binding-v1.json | cut -d ' ' -f1)"
          node scripts/kidults/source-intelligence/build-asi-requirement-adapter-coverage-semantic-input-v1.mjs \
            --source-sha "$SOURCE_SHA" --upstream-class "$UPSTREAM_CLASS" \
            --queue /tmp/arl-input/replacement-source-mission-queue-v1.json \
            --manifest /tmp/arl-input/autonomous-resolution-manifest-v1.json \
            --receipt /tmp/arl-input/kidults-asi-autonomous-resolution-layer-kpmo-receipt-v1.json \
            --contract coordination/kidults/source-intelligence/asi-requirement-adapter-coverage-contract-v1.json \
            --output "$GUARD_DIR/coverage-semantic-input-receipt-v1.json"
          CANONICAL_INPUT_DIGEST=$(jq -r '.canonical_input_digest' "$GUARD_DIR/coverage-semantic-input-receipt-v1.json")
          SEMANTIC_INPUT_RECEIPT_DIGEST="sha256:$(sha256sum "$GUARD_DIR/coverage-semantic-input-receipt-v1.json" | cut -d ' ' -f1)"
          [[ "$CANONICAL_INPUT_DIGEST" =~ ^sha256:[a-f0-9]{64}$ ]]
          READBACK_STATE=BYPASS
          READBACK_TOTAL=0
          READBACK_RETURNED=0
          PRIOR_SUCCESS_COUNT=0
          if [ "$GITHUB_EVENT_NAME" = workflow_run ]; then
            READBACK_STATE=COMPLETE
            if ! gh api --method GET -H 'Accept: application/vnd.github+json' \
              -f name="$CANONICAL_ARTIFACT_NAME" -f per_page=100 \
              "/repos/${GITHUB_REPOSITORY}/actions/artifacts" > "$GUARD_DIR/artifacts.json"; then
              READBACK_STATE=API_READBACK_FAILED
              printf '{"total_count":0,"artifacts":[]}\n' > "$GUARD_DIR/artifacts.json"
            fi
            if ! jq -e '.total_count|numbers' "$GUARD_DIR/artifacts.json" >/dev/null; then
              READBACK_STATE=API_RESPONSE_INVALID
              printf '{"total_count":0,"artifacts":[]}\n' > "$GUARD_DIR/artifacts.json"
            fi
            READBACK_TOTAL=$(jq -r '.total_count' "$GUARD_DIR/artifacts.json")
            READBACK_RETURNED=$(jq -r '.artifacts|length' "$GUARD_DIR/artifacts.json")
            if [ "$READBACK_TOTAL" -ne "$READBACK_RETURNED" ]; then READBACK_STATE=API_RESPONSE_TRUNCATED; fi
            # Canonical producer existence is proven only by exact-name artifact
            # readback. A successful classification-only workflow has no canonical
            # producer artifact and must never suppress the first producer run.
            PRIOR_SUCCESS_COUNT="$READBACK_TOTAL"
            mapfile -t ARTIFACT_ROWS < <(jq -c '.artifacts[]' "$GUARD_DIR/artifacts.json")
            for ROW in "${ARTIFACT_ROWS[@]}"; do
              ARTIFACT_ID=$(jq -r '.id // 0' <<<"$ROW")
              CANDIDATE="$CANDIDATE_DIR/candidate-${ARTIFACT_ID}.json"
              if [ "$(jq -r '.expired // true' <<<"$ROW")" = true ]; then
                jq -n --argjson artifact "$ROW" '{artifact:$artifact,run:null,receipt:null}' > "$CANDIDATE"
                continue
              fi
              RUN_ID=$(jq -r '.workflow_run.id // 0' <<<"$ROW")
              if ! gh api -H 'Accept: application/vnd.github+json' "/repos/${GITHUB_REPOSITORY}/actions/runs/${RUN_ID}" > "$CANDIDATE_DIR/run-${ARTIFACT_ID}.json"; then
                READBACK_STATE=CANONICAL_RUN_READBACK_FAILED
                jq -n --argjson artifact "$ROW" '{artifact:$artifact,run:null,receipt:null}' > "$CANDIDATE"
                continue
              fi
              DIGEST=$(jq -r '.digest // empty' <<<"$ROW")
              if ! [[ "$ARTIFACT_ID" =~ ^[1-9][0-9]*$ && "$DIGEST" =~ ^sha256:[a-f0-9]{64}$ ]]; then
                jq -n --argjson artifact "$ROW" --slurpfile run "$CANDIDATE_DIR/run-${ARTIFACT_ID}.json" '{artifact:$artifact,run:$run[0],receipt:null}' > "$CANDIDATE"
                continue
              fi
              ARCHIVE="$CANDIDATE_DIR/artifact-${ARTIFACT_ID}.zip"
              EXTRACT="$CANDIDATE_DIR/extract-${ARTIFACT_ID}"
              mkdir -p "$EXTRACT"
              if ! gh api -H 'Accept: application/vnd.github+json' "/repos/${GITHUB_REPOSITORY}/actions/artifacts/${ARTIFACT_ID}/zip" > "$ARCHIVE" || \
                 ! python3 scripts/kidults/kpmo/validate-safe-zip-archive-v1.py \
                   --archive "$ARCHIVE" --expected-digest "$DIGEST" \
                   --receipt "$CANDIDATE_DIR/archive-validation-${ARTIFACT_ID}.json" \
                   --max-compressed-bytes 4194304 --max-entries 32 \
                   --max-entry-uncompressed-bytes 2097152 --max-total-uncompressed-bytes 8388608 \
                   --max-compression-ratio 100; then
                READBACK_STATE=CANONICAL_ARTIFACT_INTEGRITY_FAILED
                jq -n --argjson artifact "$ROW" --slurpfile run "$CANDIDATE_DIR/run-${ARTIFACT_ID}.json" '{artifact:$artifact,run:$run[0],receipt:null}' > "$CANDIDATE"
                continue
              fi
              unzip -q -o "$ARCHIVE" -d "$EXTRACT"
              mapfile -d '' -t RECEIPTS < <(find "$EXTRACT" -type f -name coverage-canonical-leader-receipt-v1.json -print0)
              mapfile -d '' -t SEMANTIC_RECEIPTS < <(find "$EXTRACT" -type f -name coverage-semantic-input-receipt-v1.json -print0)
              if [ "${#RECEIPTS[@]}" -ne 1 ] || [ "${#SEMANTIC_RECEIPTS[@]}" -ne 1 ]; then
                READBACK_STATE=CANONICAL_RECEIPT_CARDINALITY_INVALID
                jq -n --argjson artifact "$ROW" --slurpfile run "$CANDIDATE_DIR/run-${ARTIFACT_ID}.json" '{artifact:$artifact,run:$run[0],receipt:null}' > "$CANDIDATE"
                continue
              fi
              SEMANTIC_FILE_DIGEST="sha256:$(sha256sum "${SEMANTIC_RECEIPTS[0]}" | cut -d ' ' -f1)"
              jq -n --argjson artifact "$ROW" --arg semanticFileDigest "$SEMANTIC_FILE_DIGEST" \
                --slurpfile run "$CANDIDATE_DIR/run-${ARTIFACT_ID}.json" --slurpfile receipt "${RECEIPTS[0]}" \
                --slurpfile semantic "${SEMANTIC_RECEIPTS[0]}" \
                '{artifact:$artifact,run:$run[0],receipt:$receipt[0],semantic_input_receipt:$semantic[0],semantic_input_receipt_file_digest:$semanticFileDigest}' > "$CANDIDATE"
            done
          fi
          if compgen -G "$CANDIDATE_DIR/candidate-*.json" >/dev/null; then jq -s '.' "$CANDIDATE_DIR"/candidate-*.json > "$GUARD_DIR/candidates.json"; else printf '[]\n' > "$GUARD_DIR/candidates.json"; fi
          if [ "$GITHUB_EVENT_NAME" = workflow_run ]; then COVERAGE_DISPLAY_TITLE="KIDULTS Coverage / source-${SOURCE_SHA}"; else COVERAGE_DISPLAY_TITLE="KIDULTS Coverage / manual-${GITHUB_RUN_ID}"; fi
          jq -n \
            --arg observed "$(date -u +%Y-%m-%dT%H:%M:%S.000Z)" --arg repository "$GITHUB_REPOSITORY" \
            --arg trigger "$GITHUB_EVENT_NAME" --argjson run "$GITHUB_RUN_ID" --argjson attempt "$GITHUB_RUN_ATTEMPT" \
            --arg source "$SOURCE_SHA" --arg class "$UPSTREAM_CLASS" --arg key "$CANONICAL_RUN_KEY" \
            --arg input "$CANONICAL_INPUT_DIGEST" --arg semanticReceipt "$SEMANTIC_INPUT_RECEIPT_DIGEST" --arg contract "$CANONICAL_CONTRACT_DIGEST" --arg binding "$UPSTREAM_BINDING_DIGEST" \
            --arg coverageConsumer "$GITHUB_SHA" --arg coverageHead "$GITHUB_SHA" --arg coverageTitle "$COVERAGE_DISPLAY_TITLE" \
            --argjson upstreamRun "$ARL_RUN_ID" --argjson upstreamArtifact "$ARL_ARTIFACT_ID" --arg upstreamDigest "$ARL_ARTIFACT_DIGEST" \
            --arg readback "$READBACK_STATE" --argjson total "$READBACK_TOTAL" --argjson returned "$READBACK_RETURNED" \
            --argjson priorSuccess "$PRIOR_SUCCESS_COUNT" \
            --slurpfile candidates "$GUARD_DIR/candidates.json" \
            '{observed_at:$observed,current:{repository:$repository,trigger_event:$trigger,run_id:$run,run_attempt:$attempt,source_sha:$source,upstream_class:$class,canonical_run_key:$key,canonical_input_digest:$input,semantic_input_receipt_digest:$semanticReceipt,canonical_contract_digest:$contract,upstream_binding_digest:$binding,upstream_workflow_run_id:$upstreamRun,upstream_artifact_id:$upstreamArtifact,upstream_artifact_digest:$upstreamDigest,coverage_consumer_sha:$coverageConsumer,coverage_run_head_sha:$coverageHead,coverage_run_display_title:$coverageTitle},readback:{state:$readback,total_count:$total,returned_count:$returned,prior_success_count:$priorSuccess,reason_codes:(if $readback=="COMPLETE" or $readback=="BYPASS" then [] else [$readback] end)},candidates:$candidates[0]}' \
            > "$GUARD_DIR/input.json"
          echo "KIDULTS_COVERAGE_CANONICAL_INPUT_DIGEST=$CANONICAL_INPUT_DIGEST" >> "$GITHUB_ENV"
          echo "KIDULTS_COVERAGE_SEMANTIC_INPUT_RECEIPT_DIGEST=$SEMANTIC_INPUT_RECEIPT_DIGEST" >> "$GITHUB_ENV"
          echo "KIDULTS_COVERAGE_CANONICAL_CONTRACT_DIGEST=$CANONICAL_CONTRACT_DIGEST" >> "$GITHUB_ENV"
          echo "KIDULTS_COVERAGE_UPSTREAM_BINDING_DIGEST=$UPSTREAM_BINDING_DIGEST" >> "$GITHUB_ENV"
          node scripts/kidults/source-intelligence/resolve-asi-requirement-adapter-coverage-canonical-guard-v1.mjs \
            --input "$GUARD_DIR/input.json" \
            --output "$GUARD_DIR/coverage-canonical-guard-receipt-v1.json" \
            --alias-output "$GUARD_DIR/coverage-canonical-alias-receipt-v1.json"
      - name: Build requirement coverage twice
        if: env.KIDULTS_COVERAGE_EXECUTE_FULL == 'true'
        run: |
          set -euo pipefail
          for RUN in 1 2; do
            node scripts/kidults/source-intelligence/build-asi-requirement-adapter-coverage-v1.mjs \
              /tmp/arl-input/replacement-source-mission-queue-v1.json \
              /tmp/arl-input/autonomous-resolution-manifest-v1.json \
              /tmp/arl-input/kidults-asi-autonomous-resolution-layer-kpmo-receipt-v1.json \
              /tmp/arl-input/autonomous-resolution-artifact-binding-v1.json \
              coordination/kidults/source-intelligence/asi-requirement-adapter-coverage-contract-v1.json \
              coordination/kidults/source-intelligence/top16-empirical-activation-preflight-v1.json \
              "/tmp/kidults-asi-requirement-adapter-coverage-run-${RUN}"
          done
          diff -qr \
            /tmp/kidults-asi-requirement-adapter-coverage-run-1 \
            /tmp/kidults-asi-requirement-adapter-coverage-run-2
      - name: Reject incomplete paired-artifact generation key mutation
        if: env.KIDULTS_COVERAGE_EXECUTE_FULL == 'true'
        run: |
          set -euo pipefail
          cp /tmp/arl-input/kidults-asi-autonomous-resolution-layer-kpmo-receipt-v1.json /tmp/bad-incomplete-generation-key-receipt-v1.json
          cp /tmp/arl-input/autonomous-resolution-artifact-binding-v1.json /tmp/bad-incomplete-generation-key-binding-v1.json
          node - <<'NODE'
          const fs=require('fs');
          const receiptPath='/tmp/bad-incomplete-generation-key-receipt-v1.json';
          const bindingPath='/tmp/bad-incomplete-generation-key-binding-v1.json';
          const receipt=JSON.parse(fs.readFileSync(receiptPath));
          const incompleteKey=`${receipt.source_sha}:${receipt.p1_workflow_run_id}:${receipt.p1_artifact_id}:${receipt.p1_artifact_digest}`;
          receipt.authoritative_generation_key=incompleteKey;
          fs.writeFileSync(receiptPath,JSON.stringify(receipt,null,2));
          const binding=JSON.parse(fs.readFileSync(bindingPath));
          binding.authoritative_generation_key=incompleteKey;
          fs.writeFileSync(bindingPath,JSON.stringify(binding,null,2));
          NODE
          if node scripts/kidults/source-intelligence/build-asi-requirement-adapter-coverage-v1.mjs \
            /tmp/arl-input/replacement-source-mission-queue-v1.json \
            /tmp/arl-input/autonomous-resolution-manifest-v1.json \
            /tmp/bad-incomplete-generation-key-receipt-v1.json \
            /tmp/bad-incomplete-generation-key-binding-v1.json \
            coordination/kidults/source-intelligence/asi-requirement-adapter-coverage-contract-v1.json \
            coordination/kidults/source-intelligence/top16-empirical-activation-preflight-v1.json \
            /tmp/bad-incomplete-generation-key-output; then
            echo 'Builder accepted a generation key that omitted the paired P0B artifact' >&2
            exit 1
          fi
      - name: Reject legacy and forged exact-trigger artifact bindings
        if: env.KIDULTS_COVERAGE_EXECUTE_FULL == 'true'
        run: |
          set -euo pipefail
          cp /tmp/arl-input/autonomous-resolution-artifact-binding-v1.json /tmp/bad-artifact-binding-version-v1.json
          cp /tmp/arl-input/autonomous-resolution-artifact-binding-v1.json /tmp/bad-artifact-binding-exact-trigger-v1.json
          node - <<'NODE'
          const fs=require('fs');
          let p='/tmp/bad-artifact-binding-version-v1.json';
          let x=JSON.parse(fs.readFileSync(p));
          x.version='1.0.0';
          fs.writeFileSync(p,JSON.stringify(x,null,2));
          p='/tmp/bad-artifact-binding-exact-trigger-v1.json';
          x=JSON.parse(fs.readFileSync(p));
          x.exact_triggering_run_bound=!x.exact_triggering_run_bound;
          fs.writeFileSync(p,JSON.stringify(x,null,2));
          NODE
          for BAD in /tmp/bad-artifact-binding-version-v1.json /tmp/bad-artifact-binding-exact-trigger-v1.json; do
            if node scripts/kidults/source-intelligence/build-asi-requirement-adapter-coverage-v1.mjs \
              /tmp/arl-input/replacement-source-mission-queue-v1.json \
              /tmp/arl-input/autonomous-resolution-manifest-v1.json \
              /tmp/arl-input/kidults-asi-autonomous-resolution-layer-kpmo-receipt-v1.json \
              "$BAD" \
              coordination/kidults/source-intelligence/asi-requirement-adapter-coverage-contract-v1.json \
              coordination/kidults/source-intelligence/top16-empirical-activation-preflight-v1.json \
              "/tmp/$(basename "$BAD" .json)-output"; then
              echo "Builder accepted stale or forged provenance binding: $BAD" >&2
              exit 1
            fi
          done
      - name: Validate all 192 requirement mappings and claim ceilings
        if: env.KIDULTS_COVERAGE_EXECUTE_FULL == 'true'
        run: |
          set -euo pipefail
          node scripts/kidults/source-intelligence/validate-asi-requirement-adapter-coverage-v1.mjs \
            /tmp/kidults-asi-requirement-adapter-coverage-run-1 \
            /tmp/arl-input/replacement-source-mission-queue-v1.json \
            /tmp/arl-input/autonomous-resolution-manifest-v1.json \
            /tmp/arl-input/kidults-asi-autonomous-resolution-layer-kpmo-receipt-v1.json \
            /tmp/arl-input/autonomous-resolution-artifact-binding-v1.json \
            coordination/kidults/source-intelligence/asi-requirement-adapter-coverage-contract-v1.json \
            coordination/kidults/source-intelligence/top16-empirical-activation-preflight-v1.json \
            | tee /tmp/kidults-asi-requirement-adapter-coverage-validation-v1.json
      - name: Reject denominator-substitution mutation
        if: env.KIDULTS_COVERAGE_EXECUTE_FULL == 'true'
        run: |
          set -euo pipefail
          cp -a /tmp/kidults-asi-requirement-adapter-coverage-run-1 /tmp/bad-coverage-denominator
          node - <<'NODE'
          const fs=require('fs');const p='/tmp/bad-coverage-denominator/requirement-adapter-coverage-ledger-v1.json';const x=JSON.parse(fs.readFileSync(p));x.requirement_count=16;x.unique_mission_count=16;fs.writeFileSync(p,JSON.stringify(x,null,2));
          NODE
          if node scripts/kidults/source-intelligence/validate-asi-requirement-adapter-coverage-v1.mjs /tmp/bad-coverage-denominator /tmp/arl-input/replacement-source-mission-queue-v1.json /tmp/arl-input/autonomous-resolution-manifest-v1.json /tmp/arl-input/kidults-asi-autonomous-resolution-layer-kpmo-receipt-v1.json /tmp/arl-input/autonomous-resolution-artifact-binding-v1.json coordination/kidults/source-intelligence/asi-requirement-adapter-coverage-contract-v1.json coordination/kidults/source-intelligence/top16-empirical-activation-preflight-v1.json; then
            echo 'Validator accepted 16-source denominator as 192-requirement accounting' >&2
            exit 1
          fi
      - name: Reject fixed or forged preflight totals mutation
        if: env.KIDULTS_COVERAGE_EXECUTE_FULL == 'true'
        run: |
          set -euo pipefail
          cp -a /tmp/kidults-asi-requirement-adapter-coverage-run-1 /tmp/bad-fixed-preflight-total
          node - <<'NODE'
          const fs=require('fs');
          const p='/tmp/bad-fixed-preflight-total/requirement-adapter-coverage-manifest-v1.json';
          const x=JSON.parse(fs.readFileSync(p));
          x.results.original_preflight_actions=672;
          x.results.terminal_preflight_actions=672;
          fs.writeFileSync(p,JSON.stringify(x,null,2));
          NODE
          if node scripts/kidults/source-intelligence/validate-asi-requirement-adapter-coverage-v1.mjs /tmp/bad-fixed-preflight-total /tmp/arl-input/replacement-source-mission-queue-v1.json /tmp/arl-input/autonomous-resolution-manifest-v1.json /tmp/arl-input/kidults-asi-autonomous-resolution-layer-kpmo-receipt-v1.json /tmp/arl-input/autonomous-resolution-artifact-binding-v1.json coordination/kidults/source-intelligence/asi-requirement-adapter-coverage-contract-v1.json coordination/kidults/source-intelligence/top16-empirical-activation-preflight-v1.json; then
            echo 'Validator accepted fixed preflight totals that conflict with authoritative ARL inputs' >&2
            exit 1
          fi
      - name: Reject legacy metric reintroduction mutation
        if: env.KIDULTS_COVERAGE_EXECUTE_FULL == 'true'
        run: |
          set -euo pipefail
          cp -a /tmp/kidults-asi-requirement-adapter-coverage-run-1 /tmp/bad-legacy-metrics
          node - <<'NODE'
          const fs=require('fs');const p='/tmp/bad-legacy-metrics/requirement-adapter-coverage-manifest-v1.json';const x=JSON.parse(fs.readFileSync(p));x.results.software_gap_requirements=153;x.results.unmapped_requirements=138;fs.writeFileSync(p,JSON.stringify(x,null,2));
          NODE
          if node scripts/kidults/source-intelligence/validate-asi-requirement-adapter-coverage-v1.mjs /tmp/bad-legacy-metrics /tmp/arl-input/replacement-source-mission-queue-v1.json /tmp/arl-input/autonomous-resolution-manifest-v1.json /tmp/arl-input/kidults-asi-autonomous-resolution-layer-kpmo-receipt-v1.json /tmp/arl-input/autonomous-resolution-artifact-binding-v1.json coordination/kidults/source-intelligence/asi-requirement-adapter-coverage-contract-v1.json coordination/kidults/source-intelligence/top16-empirical-activation-preflight-v1.json; then
            echo 'Validator accepted deprecated metrics as canonical result fields' >&2
            exit 1
          fi
      - name: Reject registered-claim inheritance mutation
        if: env.KIDULTS_COVERAGE_EXECUTE_FULL == 'true'
        run: |
          set -euo pipefail
          cp -a /tmp/kidults-asi-requirement-adapter-coverage-run-1 /tmp/bad-registered-inheritance
          node - <<'NODE'
          const fs=require('fs');const p='/tmp/bad-registered-inheritance/source-adapter-claim-ceiling-registry-v1.json';const x=JSON.parse(fs.readFileSync(p));const s=x.sources.find((v)=>v.source_id==='reverb-price-guide');s.implemented_claim_parsers=[...s.registered_claims];s.unimplemented_registered_claims=[];s.adapter_kind='TRANSACTION_PARSER';fs.writeFileSync(p,JSON.stringify(x,null,2));
          NODE
          if node scripts/kidults/source-intelligence/validate-asi-requirement-adapter-coverage-v1.mjs /tmp/bad-registered-inheritance /tmp/arl-input/replacement-source-mission-queue-v1.json /tmp/arl-input/autonomous-resolution-manifest-v1.json /tmp/arl-input/kidults-asi-autonomous-resolution-layer-kpmo-receipt-v1.json /tmp/arl-input/autonomous-resolution-artifact-binding-v1.json coordination/kidults/source-intelligence/asi-requirement-adapter-coverage-contract-v1.json coordination/kidults/source-intelligence/top16-empirical-activation-preflight-v1.json; then
            echo 'Validator accepted registered claims as implemented parsers' >&2
            exit 1
          fi
      - name: Reject context-as-parser mutation
        if: env.KIDULTS_COVERAGE_EXECUTE_FULL == 'true'
        run: |
          set -euo pipefail
          cp -a /tmp/kidults-asi-requirement-adapter-coverage-run-1 /tmp/bad-context-parser
          node - <<'NODE'
          const fs=require('fs');const p='/tmp/bad-context-parser/requirement-adapter-coverage-ledger-v1.json';const x=JSON.parse(fs.readFileSync(p));const r=x.records.find((v)=>v.software_coverage_state==='CONTEXT_ONLY');const e=r.source_evaluations.find((v)=>v.adapter_kind==='CONTEXT_ONLY_CLASSIFIER');r.software_coverage_state='SOFTWARE_IMPLEMENTED';r.qualifying_software_adapter_ids=[e.source_id];e.required_claim_parser_match=true;e.software_evaluation_state='CLAIM_PARSER_MATCH';fs.writeFileSync(p,JSON.stringify(x,null,2));
          NODE
          if node scripts/kidults/source-intelligence/validate-asi-requirement-adapter-coverage-v1.mjs /tmp/bad-context-parser /tmp/arl-input/replacement-source-mission-queue-v1.json /tmp/arl-input/autonomous-resolution-manifest-v1.json /tmp/arl-input/kidults-asi-autonomous-resolution-layer-kpmo-receipt-v1.json /tmp/arl-input/autonomous-resolution-artifact-binding-v1.json coordination/kidults/source-intelligence/asi-requirement-adapter-coverage-contract-v1.json coordination/kidults/source-intelligence/top16-empirical-activation-preflight-v1.json; then
            echo 'Validator accepted context-only classification as parser coverage' >&2
            exit 1
          fi
      - name: Reject live-rights-activation promotion mutation
        if: env.KIDULTS_COVERAGE_EXECUTE_FULL == 'true'
        run: |
          set -euo pipefail
          cp -a /tmp/kidults-asi-requirement-adapter-coverage-run-1 /tmp/bad-live-promotion
          node - <<'NODE'
          const fs=require('fs');const p='/tmp/bad-live-promotion/requirement-adapter-coverage-ledger-v1.json';const x=JSON.parse(fs.readFileSync(p));x.records[0].rights_verified=true;x.records[0].live_schema_verified=true;x.records[0].adapter_activated=true;x.records[0].evidence_admitted=true;fs.writeFileSync(p,JSON.stringify(x,null,2));
          NODE
          if node scripts/kidults/source-intelligence/validate-asi-requirement-adapter-coverage-v1.mjs /tmp/bad-live-promotion /tmp/arl-input/replacement-source-mission-queue-v1.json /tmp/arl-input/autonomous-resolution-manifest-v1.json /tmp/arl-input/kidults-asi-autonomous-resolution-layer-kpmo-receipt-v1.json /tmp/arl-input/autonomous-resolution-artifact-binding-v1.json coordination/kidults/source-intelligence/asi-requirement-adapter-coverage-contract-v1.json coordination/kidults/source-intelligence/top16-empirical-activation-preflight-v1.json; then
            echo 'Validator accepted unsupported live, rights, activation or Evidence promotion' >&2
            exit 1
          fi
      - name: Reject production authority and legacy eligibility mutations
        if: env.KIDULTS_COVERAGE_EXECUTE_FULL == 'true'
        run: |
          set -euo pipefail
          cp /tmp/arl-input/autonomous-resolution-artifact-binding-v1.json /tmp/bad-autonomous-resolution-main-scope-v1.json
          cp /tmp/arl-input/autonomous-resolution-artifact-binding-v1.json /tmp/bad-autonomous-resolution-production-authority-v1.json
          cp /tmp/arl-input/autonomous-resolution-artifact-binding-v1.json /tmp/bad-autonomous-resolution-legacy-eligibility-v1.json
          cp /tmp/arl-input/autonomous-resolution-artifact-binding-v1.json /tmp/bad-autonomous-resolution-canonical-run-key-v1.json
          node - <<'NODE'
          const fs=require('fs');
          let p='/tmp/bad-autonomous-resolution-main-scope-v1.json';
          let x=JSON.parse(fs.readFileSync(p));
          x.main_scope_validated=!x.main_scope_validated;
          fs.writeFileSync(p,JSON.stringify(x,null,2));
          p='/tmp/bad-autonomous-resolution-production-authority-v1.json';
          x=JSON.parse(fs.readFileSync(p));
          x.production_authorized=true;
          fs.writeFileSync(p,JSON.stringify(x,null,2));
          p='/tmp/bad-autonomous-resolution-legacy-eligibility-v1.json';
          x=JSON.parse(fs.readFileSync(p));
          x.production_eligible=true;
          fs.writeFileSync(p,JSON.stringify(x,null,2));
          p='/tmp/bad-autonomous-resolution-canonical-run-key-v1.json';
          x=JSON.parse(fs.readFileSync(p));
          x.canonical_run_key=`${'0'.repeat(40)}:${x.upstream_class}`;
          fs.writeFileSync(p,JSON.stringify(x,null,2));
          NODE
          for BAD in \
            /tmp/bad-autonomous-resolution-main-scope-v1.json \
            /tmp/bad-autonomous-resolution-production-authority-v1.json \
            /tmp/bad-autonomous-resolution-legacy-eligibility-v1.json \
            /tmp/bad-autonomous-resolution-canonical-run-key-v1.json; do
            if node scripts/kidults/source-intelligence/build-asi-requirement-adapter-coverage-v1.mjs \
              /tmp/arl-input/replacement-source-mission-queue-v1.json \
              /tmp/arl-input/autonomous-resolution-manifest-v1.json \
              /tmp/arl-input/kidults-asi-autonomous-resolution-layer-kpmo-receipt-v1.json \
              "$BAD" \
              coordination/kidults/source-intelligence/asi-requirement-adapter-coverage-contract-v1.json \
              coordination/kidults/source-intelligence/top16-empirical-activation-preflight-v1.json \
              "/tmp/$(basename "$BAD" .json)-output"; then
              echo "Builder accepted forbidden Production semantics: $BAD" >&2
              exit 1
            fi
          done
      - name: Reject unbound upstream digest mutation
        if: env.KIDULTS_COVERAGE_EXECUTE_FULL == 'true'
        run: |
          set -euo pipefail
          cp /tmp/arl-input/autonomous-resolution-manifest-v1.json /tmp/bad-autonomous-resolution-manifest-v1.json
          node - <<'NODE'
          const fs=require('fs');const p='/tmp/bad-autonomous-resolution-manifest-v1.json';const x=JSON.parse(fs.readFileSync(p));x.input_bindings.adapter_contract.digest='sha256:'+('0'.repeat(64));fs.writeFileSync(p,JSON.stringify(x,null,2));
          NODE
          if node scripts/kidults/source-intelligence/build-asi-requirement-adapter-coverage-v1.mjs /tmp/arl-input/replacement-source-mission-queue-v1.json /tmp/bad-autonomous-resolution-manifest-v1.json /tmp/arl-input/kidults-asi-autonomous-resolution-layer-kpmo-receipt-v1.json /tmp/arl-input/autonomous-resolution-artifact-binding-v1.json coordination/kidults/source-intelligence/asi-requirement-adapter-coverage-contract-v1.json coordination/kidults/source-intelligence/top16-empirical-activation-preflight-v1.json /tmp/bad-unbound-digest-output; then
            echo 'Builder accepted an unbound upstream digest' >&2
            exit 1
          fi
      - name: Reject silent-drop and duplicate-grain mutations
        if: env.KIDULTS_COVERAGE_EXECUTE_FULL == 'true'
        run: |
          set -euo pipefail
          cp /tmp/arl-input/replacement-source-mission-queue-v1.json /tmp/bad-silent-drop-queue.json
          cp /tmp/arl-input/replacement-source-mission-queue-v1.json /tmp/bad-duplicate-grain-queue.json
          node - <<'NODE'
          const fs=require('fs');let p='/tmp/bad-silent-drop-queue.json';let x=JSON.parse(fs.readFileSync(p));x.missions.pop();x.mission_count=191;fs.writeFileSync(p,JSON.stringify(x,null,2));p='/tmp/bad-duplicate-grain-queue.json';x=JSON.parse(fs.readFileSync(p));x.missions[1].mission_id=x.missions[0].mission_id;fs.writeFileSync(p,JSON.stringify(x,null,2));
          NODE
          for BAD in /tmp/bad-silent-drop-queue.json /tmp/bad-duplicate-grain-queue.json; do
            if node scripts/kidults/source-intelligence/build-asi-requirement-adapter-coverage-v1.mjs "$BAD" /tmp/arl-input/autonomous-resolution-manifest-v1.json /tmp/arl-input/kidults-asi-autonomous-resolution-layer-kpmo-receipt-v1.json /tmp/arl-input/autonomous-resolution-artifact-binding-v1.json coordination/kidults/source-intelligence/asi-requirement-adapter-coverage-contract-v1.json coordination/kidults/source-intelligence/top16-empirical-activation-preflight-v1.json "/tmp/$(basename "$BAD" .json)-output"; then
              echo "Builder accepted forbidden mission mutation: $BAD" >&2
              exit 1
            fi
          done
      - name: Reject unaccountable or non-idempotent gap queue mutations
        if: env.KIDULTS_COVERAGE_EXECUTE_FULL == 'true'
        run: |
          set -euo pipefail
          cp -a /tmp/kidults-asi-requirement-adapter-coverage-run-1 /tmp/bad-gap-owner
          cp -a /tmp/kidults-asi-requirement-adapter-coverage-run-1 /tmp/bad-gap-idempotency
          cp -a /tmp/kidults-asi-requirement-adapter-coverage-run-1 /tmp/bad-gap-fallback
          node - <<'NODE'
          const fs=require('fs');
          let p='/tmp/bad-gap-owner/requirement-adapter-gap-queue-v1.json';
          let x=JSON.parse(fs.readFileSync(p));
          x.records[0].accountable_owner='';
          fs.writeFileSync(p,JSON.stringify(x,null,2));
          p='/tmp/bad-gap-idempotency/requirement-adapter-gap-queue-v1.json';
          x=JSON.parse(fs.readFileSync(p));
          x.records[1].idempotency_key=x.records[0].idempotency_key;
          fs.writeFileSync(p,JSON.stringify(x,null,2));
          p='/tmp/bad-gap-fallback/requirement-adapter-gap-queue-v1.json';
          x=JSON.parse(fs.readFileSync(p));
          x.work_units[0].fallback_path.external_contact_authorized=true;
          fs.writeFileSync(p,JSON.stringify(x,null,2));
          NODE
          for BAD in /tmp/bad-gap-owner /tmp/bad-gap-idempotency /tmp/bad-gap-fallback; do
            if node scripts/kidults/source-intelligence/validate-asi-requirement-adapter-coverage-v1.mjs "$BAD" /tmp/arl-input/replacement-source-mission-queue-v1.json /tmp/arl-input/autonomous-resolution-manifest-v1.json /tmp/arl-input/kidults-asi-autonomous-resolution-layer-kpmo-receipt-v1.json /tmp/arl-input/autonomous-resolution-artifact-binding-v1.json coordination/kidults/source-intelligence/asi-requirement-adapter-coverage-contract-v1.json coordination/kidults/source-intelligence/top16-empirical-activation-preflight-v1.json; then
              echo "Validator accepted an unaccountable, duplicate, or externally authorized gap queue: $BAD" >&2
              exit 1
            fi
          done
      - name: Reject manual-only activation mutation
        if: env.KIDULTS_COVERAGE_EXECUTE_FULL == 'true'
        run: |
          set -euo pipefail
          cp coordination/kidults/source-intelligence/asi-requirement-adapter-coverage-registry-v1.json /tmp/coverage-registry-pristine.json
          restore_registry() { mv /tmp/coverage-registry-pristine.json coordination/kidults/source-intelligence/asi-requirement-adapter-coverage-registry-v1.json; }
          trap restore_registry EXIT
          node - <<'NODE'
          const fs=require('fs');const p='coordination/kidults/source-intelligence/asi-requirement-adapter-coverage-registry-v1.json';const x=JSON.parse(fs.readFileSync(p));x.automatic_activation.main_push=false;x.automatic_activation.schedule='NONE';x.automatic_activation.upstream_workflows=[];fs.writeFileSync(p,JSON.stringify(x,null,2));
          NODE
          if node scripts/kidults/source-intelligence/validate-asi-requirement-adapter-coverage-registry-v1.mjs; then
            echo 'Registry validator accepted manual-only activation' >&2
            exit 1
          fi
          restore_registry
          trap - EXIT
      - name: Revalidate pristine coverage outputs and registry
        if: env.KIDULTS_COVERAGE_EXECUTE_FULL == 'true'
        run: |
          set -euo pipefail
          node scripts/kidults/source-intelligence/validate-asi-requirement-adapter-coverage-registry-v1.mjs \
            > /tmp/kidults-asi-requirement-adapter-coverage-registry-final-validation-v1.json
          node scripts/kidults/source-intelligence/validate-asi-requirement-adapter-coverage-v1.mjs \
            /tmp/kidults-asi-requirement-adapter-coverage-run-1 \
            /tmp/arl-input/replacement-source-mission-queue-v1.json \
            /tmp/arl-input/autonomous-resolution-manifest-v1.json \
            /tmp/arl-input/kidults-asi-autonomous-resolution-layer-kpmo-receipt-v1.json \
            /tmp/arl-input/autonomous-resolution-artifact-binding-v1.json \
            coordination/kidults/source-intelligence/asi-requirement-adapter-coverage-contract-v1.json \
            coordination/kidults/source-intelligence/top16-empirical-activation-preflight-v1.json \
            > /tmp/kidults-asi-requirement-adapter-coverage-final-validation-v1.json
      - name: Emit KPMO requirement coverage receipt
        if: env.KIDULTS_COVERAGE_EXECUTE_FULL == 'true'
        run: |
          node - <<'NODE' > /tmp/kidults-asi-requirement-adapter-coverage-kpmo-receipt-v1.json
          const fs=require('fs');const crypto=require('crypto');const p='/tmp/kidults-asi-requirement-adapter-coverage-run-1/requirement-adapter-coverage-manifest-v1.json';const m=JSON.parse(fs.readFileSync(p));const binding=JSON.parse(fs.readFileSync('/tmp/arl-input/autonomous-resolution-artifact-binding-v1.json'));const digest='sha256:'+crypto.createHash('sha256').update(fs.readFileSync(p)).digest('hex');const out={id:'kidults-asi-requirement-adapter-coverage-kpmo-receipt-v1',version:'1.3.0',agent_id:'github-actions/kidults-asi-requirement-adapter-coverage-v1',as_of:new Date().toISOString(),scope:'192 autonomous-resolution missions to current 16-source implemented claim ceilings',state:'VERIFIED_PASS_INTERNAL_QUEUE_ACCOUNTABLE_EXTERNAL_ACTIVATION_HOLD',facts:[`requirements_accounted_for=${m.results.requirements_accounted_for}`,`original_preflight_actions=${m.results.original_preflight_actions}`,`terminal_preflight_actions=${m.results.terminal_preflight_actions}`,`unresolved_preflight_actions=${m.results.unresolved_preflight_actions}`,`source_profile_discovery_requirements=${m.results.source_profile_discovery_requirements}`,`schema_bound_claim_parser_requirements=${m.results.schema_bound_claim_parser_requirements}`,`source_discovery_work_units=${m.results.source_discovery_work_units}`,`schema_bound_source_claim_work_units=${m.results.schema_bound_source_claim_work_units}`,`canonical_run_key=${binding.canonical_run_key}`],evidence_refs:[`workflow_run:${process.env.GITHUB_RUN_ID}`,`source_artifact_run:${binding.workflow_run_id}`,`source_artifact:${binding.artifact_id}`,`upstream_class:${binding.upstream_class}`,`canonical_run_key:${binding.canonical_run_key}`,`source_sha:${m.source_sha}`,`consumer_sha:${m.consumer_sha}`,`manifest_digest:${digest}`],inferences:[],uncertainties:[],blockers:['SOURCE_PROFILE_DISCOVERY_RIGHTS_LIVE_SCHEMA_SEMANTICS_ORIGIN_ACTIVATION_AND_EMPIRICAL_ADMISSION_REMAIN_HOLD'],actions_executed:['RESTORED_EXACT_SUCCESSFUL_PROVENANCE_BOUND_ARL_ARTIFACT','VERIFIED_CANONICAL_SOURCE_SHA_AND_UPSTREAM_CLASS_FANOUT_IDENTITY',`VERIFIED_ALL_${m.results.terminal_preflight_actions}_PREFLIGHT_ACTIONS_TERMINAL`,'BUILT_REQUIREMENT_CROSSWALK_TWICE','CLASSIFIED_120_SOURCE_DISCOVERY_AND_33_SCHEMA_BOUND_REQUIREMENTS','BOUND_153_GAPS_TO_42_DISCOVERY_AND_10_SCHEMA_WORK_UNITS','VALIDATED_OWNER_SLA_IDEMPOTENCY_AND_GENERIC_FALLBACK_COVERAGE','VALIDATED_192_GRAINS_AND_16_FAMILIES','EXECUTED_FAIL_CLOSED_MUTATIONS','ISOLATED_DEPRECATED_METRICS_FROM_CANONICAL_RESULTS'],next_action:'EXECUTE_INTERNAL_WORK_UNITS_WITHOUT_EXTERNAL_CONTACT_AND REQUIRE RIGHTS_PLUS_LIVE_SCHEMA BEFORE CLAIM_PARSER_ACTIVATION',authority_boundary:'REVERSIBLE_INTERNAL_NON_PRODUCTION_SOFTWARE_LINEAGE_ONLY',source_sha:m.source_sha,consumer_sha:m.consumer_sha,artifact_binding_version:binding.version,upstream_class:binding.upstream_class,canonical_run_key:binding.canonical_run_key,upstream_workflow_run_id:binding.workflow_run_id,upstream_artifact_id:binding.artifact_id,manifest_digest:digest,results:m.results,main_scope_validated:m.main_scope_validated,production_authorized:false,autonomous_effect:m.autonomous_effect,global_effect:m.global_effect,irreplaceable_value_effect:m.irreplaceable_value_effect,transparency_effect:m.transparency_effect,live_source_requests_executed:0,provider_contacts_executed:0,rights_passes_created:0,adapters_activated:0,evidence_admitted:0,market_events_created:0,snapshot_candidates_created:0,track_b_results_created:0,projections_created:0,public_release:'HOLD',production:'HOLD',g5:'HOLD'};process.stdout.write(JSON.stringify(out,null,2)+'\n');
          NODE
          node - <<'NODE'
          const fs=require('fs');
          const p='/tmp/kidults-asi-requirement-adapter-coverage-kpmo-receipt-v1.json';
          const x=JSON.parse(fs.readFileSync(p,'utf8'));
          x.canonical_identity={canonical_run_key:x.canonical_run_key,canonical_input_digest:process.env.KIDULTS_COVERAGE_CANONICAL_INPUT_DIGEST,semantic_input_receipt_digest:process.env.KIDULTS_COVERAGE_SEMANTIC_INPUT_RECEIPT_DIGEST,canonical_contract_digest:process.env.KIDULTS_COVERAGE_CANONICAL_CONTRACT_DIGEST,upstream_binding_digest:process.env.KIDULTS_COVERAGE_UPSTREAM_BINDING_DIGEST,ephemeral_actions_leader:process.env.KIDULTS_COVERAGE_EPHEMERAL_LEADER==='true',claim_scope:'EPHEMERAL_ACTIONS_ARTIFACT_90_DAY',runtime_dedupe_state:'REMOTE_LEDGER_ACTIVATION_HOLD',canonical_execution_claimed:false,durable_claim_created:false,every_noncanonical_trigger_alias_receipt_guaranteed:false,pending_replacement_caveat:'GITHUB_CONCURRENCY_ONE_RUNNING_ONE_PENDING_ADDITIONAL_PENDING_MAY_BE_REPLACED'};
          x.upstream_artifact_digest=process.env.ARL_ARTIFACT_DIGEST;
          x.archive_validation_receipt_digest=process.env.ARL_ARCHIVE_VALIDATION_RECEIPT_DIGEST;
          fs.writeFileSync(p,JSON.stringify(x,null,2)+'\n');
          NODE
          cat /tmp/kidults-asi-requirement-adapter-coverage-kpmo-receipt-v1.json
      - name: Finalize verified Coverage canonical leader receipt
        if: env.KIDULTS_COVERAGE_EXECUTE_FULL == 'true' && env.KIDULTS_COVERAGE_EPHEMERAL_LEADER == 'true'
        run: |
          set -euo pipefail
          GUARD_DIR=/tmp/kidults-coverage-canonical-guard
          jq \
            --arg observed "$(date -u +%Y-%m-%dT%H:%M:%S.000Z)" \
            --arg manifest "sha256:$(sha256sum /tmp/kidults-asi-requirement-adapter-coverage-run-1/requirement-adapter-coverage-manifest-v1.json | cut -d ' ' -f1)" \
            --arg kpmo "sha256:$(sha256sum /tmp/kidults-asi-requirement-adapter-coverage-kpmo-receipt-v1.json | cut -d ' ' -f1)" \
            --arg archive "$ARL_ARCHIVE_VALIDATION_RECEIPT_DIGEST" \
            --arg guard "$KIDULTS_COVERAGE_GUARD_RECEIPT_DIGEST" \
            '.current + {observed_at:$observed,coverage_manifest_digest:$manifest,coverage_kpmo_receipt_digest:$kpmo,archive_validation_receipt_digest:$archive,guard_receipt_digest:$guard}' \
            "$GUARD_DIR/input.json" > "$GUARD_DIR/finalize-input.json"
          node scripts/kidults/source-intelligence/resolve-asi-requirement-adapter-coverage-canonical-guard-v1.mjs \
            --mode finalize --input "$GUARD_DIR/finalize-input.json" \
            --output "$GUARD_DIR/coverage-canonical-leader-receipt-v1.json"
      - name: Upload exact-run Coverage or verified alias packet
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02
        with:
          name: kidults-asi-requirement-adapter-coverage-v1
          path: |
            /tmp/kidults-asi-requirement-adapter-coverage-run-1
            /tmp/arl-input/autonomous-resolution-artifact-binding-v1.json
            /tmp/coverage-exact-upstream-producer-receipt-v1.json
            /tmp/arl-archive-validation-receipt-v1.json
            /tmp/kidults-coverage-canonical-guard
            /tmp/kidults-asi-requirement-adapter-coverage-registry-validation-v1.json
            /tmp/kidults-asi-requirement-adapter-coverage-validation-v1.json
            /tmp/kidults-asi-requirement-adapter-coverage-registry-final-validation-v1.json
            /tmp/kidults-asi-requirement-adapter-coverage-final-validation-v1.json
            /tmp/kidults-asi-requirement-adapter-coverage-kpmo-receipt-v1.json
          retention-days: 90
          if-no-files-found: error
      - name: Publish successful bounded Coverage canonical leader artifact
        if: success() && env.KIDULTS_COVERAGE_EXECUTE_FULL == 'true' && env.KIDULTS_COVERAGE_EPHEMERAL_LEADER == 'true'
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02
        with:
          name: ${{ env.KIDULTS_COVERAGE_CANONICAL_ARTIFACT_NAME }}
          path: |
            /tmp/kidults-coverage-canonical-guard/coverage-canonical-leader-receipt-v1.json
            /tmp/kidults-coverage-canonical-guard/coverage-canonical-guard-receipt-v1.json
            /tmp/kidults-coverage-canonical-guard/coverage-semantic-input-receipt-v1.json
            /tmp/kidults-asi-requirement-adapter-coverage-kpmo-receipt-v1.json
          retention-days: 90
          if-no-files-found: error

  dispatch-kir-coverage-assurance:
    needs: [classify-upstream-arl-generation, verify-requirement-adapter-coverage]
    if: >-
      always() && github.event_name == 'workflow_run' &&
      needs.classify-upstream-arl-generation.result == 'success' &&
      needs.classify-upstream-arl-generation.outputs.should_run == 'true' &&
      needs.verify-requirement-adapter-coverage.result == 'success'
    runs-on: ubuntu-24.04
    timeout-minutes: 10
    permissions:
      contents: read
      actions: write
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
        with:
          ref: ${{ github.sha }}
          fetch-depth: 0
          persist-credentials: false
      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020
        with:
          node-version: '24.19.0'
          package-manager-cache: false
      - name: Issue exact Coverage assurance continuation receipt
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          test "$GITHUB_REF" = refs/heads/main
          test "$(git rev-parse HEAD)" = "$GITHUB_SHA"
          SOURCE_TREE=$(git rev-parse 'HEAD^{tree}')
          gh api -H 'Accept: application/vnd.github+json' \
            "/repos/${GITHUB_REPOSITORY}/actions/runs/${GITHUB_RUN_ID}" \
            > /tmp/kir-coverage-run.json
          jq -n \
            --arg repository "$GITHUB_REPOSITORY" --arg sha "$GITHUB_SHA" --arg tree "$SOURCE_TREE" \
            --argjson run "$GITHUB_RUN_ID" --argjson attempt "$GITHUB_RUN_ATTEMPT" \
            --slurpfile runRecord /tmp/kir-coverage-run.json \
            '{repository:$repository,source_sha:$sha,source_tree:$tree,coverage_run_id:$run,coverage_run_attempt:$attempt,run:$runRecord[0]}' \
            > /tmp/kir-coverage-assurance-issue-input-v1.json
          node scripts/kidults/kpmo/validate-kir-coverage-assurance-continuation-v1.mjs issue \
            --input /tmp/kir-coverage-assurance-issue-input-v1.json \
            --output /tmp/kir-coverage-assurance-continuation-v1.json
      - name: Retain exact Coverage assurance continuation receipt
        id: dispatch_archive
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02
        with:
          name: kidults-kir-coverage-assurance-dispatch-v1-${{ github.run_id }}-${{ github.run_attempt }}
          path: /tmp/kir-coverage-assurance-continuation-v1.json
          retention-days: 90
          if-no-files-found: error
      - name: Dispatch exact Coverage consumer and Assurance
        env:
          GH_TOKEN: ${{ github.token }}
          DISPATCH_ARTIFACT_ID: ${{ steps.dispatch_archive.outputs.artifact-id }}
          DISPATCH_ARTIFACT_DIGEST_RAW: ${{ steps.dispatch_archive.outputs.artifact-digest }}
        run: |
          set -euo pipefail
          [[ "$DISPATCH_ARTIFACT_ID" =~ ^[1-9][0-9]*$ ]]
          DISPATCH_ARTIFACT_DIGEST="$DISPATCH_ARTIFACT_DIGEST_RAW"
          if [[ "$DISPATCH_ARTIFACT_DIGEST" =~ ^[a-f0-9]{64}$ ]]; then
            DISPATCH_ARTIFACT_DIGEST="sha256:${DISPATCH_ARTIFACT_DIGEST}"
          fi
          [[ "$DISPATCH_ARTIFACT_DIGEST" =~ ^sha256:[a-f0-9]{64}$ ]]
          SOURCE_TREE=$(jq -r .source_tree /tmp/kir-coverage-assurance-continuation-v1.json)
          COVERAGE_CREATED_AT=$(jq -r .coverage_created_at /tmp/kir-coverage-assurance-continuation-v1.json)
          CONTINUATION_KEY=$(jq -r .continuation_key /tmp/kir-coverage-assurance-continuation-v1.json)
          jq -n --arg ref main --arg profile sentinel \
            --arg run "$GITHUB_RUN_ID" --arg attempt "$GITHUB_RUN_ATTEMPT" \
            --arg sha "$GITHUB_SHA" --arg tree "$SOURCE_TREE" --arg created "$COVERAGE_CREATED_AT" \
            --arg artifact "$DISPATCH_ARTIFACT_ID" --arg digest "$DISPATCH_ARTIFACT_DIGEST" --arg key "$CONTINUATION_KEY" \
            '{ref:$ref,inputs:{profile:$profile,coverage_run_id:$run,coverage_run_attempt:$attempt,coverage_source_sha:$sha,coverage_source_tree:$tree,coverage_created_at:$created,coverage_dispatch_artifact_id:$artifact,coverage_dispatch_artifact_digest:$digest,coverage_continuation_key:$key}}' \
            > /tmp/kir-coverage-assurance-dispatch-request-v1.json
          gh api --method POST -H 'Accept: application/vnd.github+json' \
            "/repos/${GITHUB_REPOSITORY}/actions/workflows/kidults-platform-continuous-assurance-v1.yml/dispatches" \
            --input /tmp/kir-coverage-assurance-dispatch-request-v1.json
