name: KIDULTS DigitalOcean Read-Only Audit

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled-pending-exact-main-approval:
    runs-on: ubuntu-24.04
    timeout-minutes: 1
    steps:
      - name: Provider execution is hard-disabled
        shell: bash
        run: |
          echo "DISABLED_PENDING_EXACT_MAIN_APPROVAL"
          exit 1
