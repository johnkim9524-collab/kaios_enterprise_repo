name: KIDULTS Autonomous Met Open Access Sample

on:
  push:
    branches:
      - "feat/kidults-phase2-content-data-provider-foundation"
    paths:
      - "coordination/kidults/autonomous/source-discovery/contracts/met-costume-open-access-r1.json"
      - "scripts/kidults/autonomous/collect-met-open-access-sample.mjs"
      - "scripts/kidults/autonomous/validate-met-open-access-sample.mjs"
      - ".github/workflows/kidults-autonomous-met-sample.yml"
  schedule:
    - cron: "30 22 * * *"
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: kidults-autonomous-met-sample
  cancel-in-progress: true

jobs:
  collect:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Set up Node.js
        uses: actions/setup-node@v7
        with:
          node-version: "24"

      - name: Syntax check
        run: |
          node --check scripts/kidults/autonomous/collect-met-open-access-sample.mjs
          node --check scripts/kidults/autonomous/validate-met-open-access-sample.mjs

      - name: Collect bounded live metadata sample
        run: |
          node scripts/kidults/autonomous/collect-met-open-access-sample.mjs \
            --output artifacts/autonomous-source-samples/met-costume-open-access-r1 \
            --query dress \
            --limit 12 \
            --minimum-records 8

      - name: Validate evidence and safety gates
        run: |
          node scripts/kidults/autonomous/validate-met-open-access-sample.mjs \
            artifacts/autonomous-source-samples/met-costume-open-access-r1

      - name: Upload evidence artifact
        if: always()
        uses: actions/upload-artifact@v6
        with:
          name: kidults-met-open-access-bounded-sample
          path: artifacts/autonomous-source-samples/met-costume-open-access-r1
          if-no-files-found: error
          retention-days: 30
