#!/usr/bin/env node
console.error('CLOUDFLARE_GLOBAL_NO_RERUN: Worker deploy and remote D1 mutation are disabled until the external durable trust root, signed readback, and deployed negative canaries are verified.');
process.exit(78);
